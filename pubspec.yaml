name: dania
publish_to: 'none'
description: Danîa✨ — personnalisation de fonds d'écran.
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.0
  image_gallery_saver_plus: ^5.1.1
  flutter_wallpaper_plus: ^1.1.1
  google_fonts: ^6.2.1
  path_provider: ^2.1.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/dania_logo.png
